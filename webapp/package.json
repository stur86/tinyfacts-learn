{
  "name": "tinyfacts-webapp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Client-only browser inference for tinyfacts-learn models, via ONNX Runtime Web.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "onnxruntime-web": "^1.27.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
